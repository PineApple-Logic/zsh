## ex - archive extractor
# usage: ex <file>
ex ()
{
  if [ -f $1 ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1   ;;
      *.tar.gz)    tar xzf $1   ;;
      *.tar.xz)    tar xJf $1   ;;
      *.bz2)       bunzip2 $1   ;;
      *.rar)       unrar x $1   ;;
      *.gz)        gunzip $1    ;;
      *.tar)       tar xf $1    ;;
      *.tbz2)      tar xjf $1   ;;
      *.tgz)       tar xzf $1   ;;
      *.zip)       unzip $1     ;;
      *.Z)         uncompress $1;;
      *.7z)        7z x $1      ;;
      *)           echo "'$1' cannot be extracted via ex()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}

export EDITOR=nano
export PATH=$PATH:~/.cargo/bin/
export PATH=$PATH:/usr/local/go/bin
export PATH="$PATH:/opt/nvim-linux64/bin"
export PATH="$PATH:/home/pineapplelogic/.local/pipx/venvs/"

## ls aliases
alias ll='ls -l'
alias l='ls -lFh'     #size,show type,human readable
alias la='ls -lAFh'   #long list,show almost all,show type,human readable
alias lr='ls -tRFh'   #sorted by date,recursive,show type,human readable
alias lt='ls -ltFh'   #long list,sorted by date,show type,human readable
alias ll='ls -l'      #long list
alias ldot='ls -ld .*'
alias lS='ls -1FSsh'
alias lart='ls -1Fcart'
alias lrt='ls -1Fcrt'
alias batcat='cat'

## Hackers aliases
alias workspace='cp -fr /home/pineapplelogic/Documents/Notes/Templates/Pen-200/Target/* ./ && cp -fr /home/pineapplelogic/Documents/Notes/Templates/Pen-200/Target/.obsidian ./ && export wg=$(pwd)'
alias offsec='sudo openvpn /home/pineapplelogic/.VPN/universal.ovpn'
alias htb='sudo openvpn /home/pineapplelogic/.VPN/lab_PineAppleHack.ovpn'
alias tryhackme='sudo openvpn /home/pineapplelogic/.VPN/PineAppleLogic.ovpn'
alias autorecon-ng='sudo $(which autorecon)'
alias winencode='python3 /home/pineapplelogic/Documents/Scripts/Windows/my_scripts/encode.py'
alias linencode='python3 /home/pineapplelogic/Documents/Scripts/linux/my_scripts/encode.py'
alias st='subl'
# alias listener='bash -c "rlwrap -CAr ncat -lvnp"' # Replaced with function
alias webserver='uploadserver'
alias nmap-scan='sudo grc nmap -T4 -p - -sC -sV -sS -vv --open -oN nmap-TCP-scan.txt'
alias ligolo-startup='sudo ip tuntap add user pineapplelogic mode tun ligolo && sudo ip link set ligolo up'
alias ligolo-ng='sudo /opt/ligolo/proxy -selfcert'
alias winscripts='cd ~/Documents/Scripts/Windows'
alias upgrade-shell='`stty size;stty raw -echo;fg`'
alias kerbrute='/home/pineapplelogic/go/bin/kerbrute'
alias bloodhound='cd /opt/BloodHound && sudo docker-compose up'


## Hackers Verables
export rockyou='/usr/share/wordlists/rockyou.txt'
